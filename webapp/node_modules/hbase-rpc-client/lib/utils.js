// Generated by CoffeeScript 1.12.7
(function() {
  module.exports = {
    bufferIncrement: function(buffer, i) {
      var b, tmp;
      b = new Buffer(buffer);
      if (i == null) {
        i = b.length - 1;
      }
      if (i < 0) {
        return Buffer.concat([new Buffer([1]), b]);
      }
      tmp = new Buffer([parseInt(b[i]++)]);
      if (tmp[0] > b[i]) {
        return this.bufferIncrement(b, i - 1);
      }
      return b;
    },
    bufferDecrement: function(buffer, i) {
      var b, tmp;
      b = new Buffer(buffer);
      if (!b.length) {
        return b;
      }
      if (i == null) {
        i = b.length - 1;
      }
      tmp = new Buffer([parseInt(b[i]--)]);
      if (tmp[0] < b[i]) {
        return this.bufferDecrement(b.slice(0, b.length - 1), i - 1);
      }
      return b;
    },
    bufferCompare: function(a, b) {
      var _a, _b, i, j, len1, len2, ref;
      len1 = a.length;
      len2 = b.length;
      if (a === b && len1 === len2) {
        return 0;
      }
      for (i = j = 0, ref = len1 - 1; 0 <= ref ? j <= ref : j >= ref; i = 0 <= ref ? ++j : --j) {
        if (a[i] !== b[i]) {
          _a = a[i] ? a[i] : 0;
          _b = b[i] ? b[i] : 0;
          return _a - _b;
        }
      }
      return len1 - len2;
    }
  };

}).call(this);
